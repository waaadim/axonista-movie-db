## Build and Run app
1. install npm dependencies
    ```bash
    npm install
    ```
2. Update `host` and `password` in `bsconfig.json`
3. Build a zip of the project
    ```bash
    npm run build
    ```
